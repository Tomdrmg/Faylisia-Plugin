package fr.blockincraft.faylisia.items.enchantment;

import fr.blockincraft.faylisia.items.CustomItem;
import org.bukkit.Material;

/**
 * We create a new class in order to use 'instanceof' function
 */
public class EnchantmentLacryma extends CustomItem {
    public EnchantmentLacryma(Material material, String id) {
        super(material, id);
    }
}
